<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
      <artifactId>ma-dashboards-parent</artifactId>
      <groupId>com.infiniteautomation.mango</groupId>
      <version>3.3.0-SNAPSHOT</version>
      <relativePath>../pom.xml</relativePath>
    </parent>
    <artifactId>ui</artifactId>
    <name>mangoUI</name>
    <version>3.3.0-SNAPSHOT</version>
    <build>
      <plugins>
          <plugin>
              <groupId>org.codehaus.gmavenplus</groupId>
              <artifactId>gmavenplus-plugin</artifactId>
          </plugin>
          <plugin>
              <artifactId>maven-war-plugin</artifactId>
          </plugin>
          <plugin>
              <artifactId>maven-assembly-plugin</artifactId>
          </plugin>
          <plugin>
              <artifactId>maven-antrun-plugin</artifactId>
          </plugin>
          <plugin>
              <groupId>com.github.eirslett</groupId>
              <artifactId>frontend-maven-plugin</artifactId>
              <version>1.4</version>
              <executions>
                  <execution>
                      <id>Install node and npm</id>
                      <goals>
                          <goal>install-node-and-npm</goal>
                      </goals>
                      <configuration>
                          <nodeVersion>v6.11.0</nodeVersion>
                      </configuration>
                  </execution>
                  <execution>
                      <id>Run npm install</id>
                      <goals>
                          <goal>npm</goal>
                      </goals>
                      <configuration>
                          <arguments>install</arguments>
                      </configuration>
                  </execution>
                  <execution>
                      <id>Run Bower install</id>
                      <goals>
                          <goal>bower</goal>
                      </goals>
                      <configuration>
                          <arguments>install</arguments>
                      </configuration>
                  </execution>
                  <execution>
                      <id>Run gulp copy-vendor</id>
                      <goals>
                          <goal>gulp</goal>
                      </goals>
                      <configuration>
                          <arguments>copy-vendor</arguments>
                          <!-- srcdir and outputdir only affect Eclipse -->
                          <srcdir>${project.basedir}/bower_components</srcdir>
                          <outputdir>${project.basedir}/web/vendor</outputdir>
                      </configuration>
                  </execution>
                  <execution>
                      <id>Run gulp copy-docs</id>
                      <goals>
                          <goal>gulp</goal>
                      </goals>
                      <configuration>
                          <arguments>copy-docs</arguments>
                          <!-- srcdir and outputdir only affect Eclipse -->
                          <srcdir>${project.basedir}/web/ngMango</srcdir>
                          <outputdir>${project.basedir}/web/ui/views/docs</outputdir>
                      </configuration>
                  </execution>
              </executions>
          </plugin>
      </plugins>
    </build>
    <description>Provides an AngularJS user interface, including the ability to create custom pages</description>
</project>
